<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Dias' - solution pour une Afrique indépendante" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Dias" %>">
    <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <% if user_signed_in? %>
       <div class="container">
         <%= render 'layouts/messages' %>
       </div>
          <div class="container">
            <% if current_user.nonvalidated %><%= render 'layouts/nonvalidated' %><% end %>
            <%= yield %>
          </div>
        
        <% else %>
           <div class="container">
              <%= render 'layouts/messages' %>
          </div>
          <%= yield %>
          <% end %>

     </div>
    </main>

    <!-- GOOGLE ANALYTICS -->

    <script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-52373443-1', 'auto');
  ga('send', 'pageview');

</script>
  </body>
</html>
